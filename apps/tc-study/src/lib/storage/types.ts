/**
 * Storage-related types for tc-study.
 * Re-export WorkspacePackage as ResourcePackage for backward compatibility.
 */
export type { WorkspacePackage as ResourcePackage } from '../stores/workspaceStore'
