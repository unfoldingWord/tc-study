/**
 * Durable-ish content identity from catalog metadata.
 * Re-exported from @bt-synergy/resource-catalog (shared with loaders).
 */
export {
  resourceContentStamp,
  type ResourceStampSource,
} from '@bt-synergy/resource-catalog'
