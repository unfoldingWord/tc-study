/**
 * Legacy rollback helper. Prefer UsjScriptureViewModel tokens from loadScriptureResult
 * (alignedOriginalWordIds already attached). Re-export for any dual-read callers.
 */
export { attachAlignmentSemanticIds } from '@bt-synergy/usj-processor'
