/**
 * Default Passage Sets Modal Component
 */

// TODO: Implement DefaultPassageSetsModal
export function DefaultPassageSetsModal({ onClose, onSelect, manager, onImported }: { onClose: () => void; onSelect?: (set: any) => void; manager?: any; onImported?: () => Promise<void> }) {
  return null
}
