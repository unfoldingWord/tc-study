/**
 * Settings Screen
 * 
 * Equivalent to bt-studio's Settings component.
 */

import { Settings } from '@/lib/components/Settings';
import React from 'react';

export default function SettingsScreen() {
  return <Settings />;
}
