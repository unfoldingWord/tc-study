/**
 * App re-export of canonical resource type IDs.
 * SoT: `@bt-synergy/resource-catalog` (`resourceTypeIds.ts`).
 */
export {
  RESOURCE_TYPE_IDS,
  getResourceTypeDisplayName,
  isValidResourceTypeId,
  type ResourceTypeId,
} from '@bt-synergy/resource-catalog'