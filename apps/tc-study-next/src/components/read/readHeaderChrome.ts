/**
 * Shared Read panel header icon button. Same treatment on both panels
 * (no panel-tint hover/icon). Compact width so tabs keep the row.
 * 36×36 stays tappable; do not force 44×44 width.
 */
export const READ_HEADER_ICON_BUTTON =
  'flex items-center justify-center rounded p-1 text-fg-secondary hover:bg-muted transition-colors h-9 w-9 shrink-0'
