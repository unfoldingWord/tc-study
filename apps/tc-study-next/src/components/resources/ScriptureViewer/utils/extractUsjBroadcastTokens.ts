/**
 * Re-export canonical SCRIPTURE_TOKENS projection from scripture-loader.
 * Kept as a stable import path for ScriptureViewer hooks.
 */

export {
  extractUsjBroadcastTokens,
  type BroadcastScriptureToken,
} from '@bt-synergy/scripture-loader'
