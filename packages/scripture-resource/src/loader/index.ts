/**
 * Scripture Loader
 * 
 * Re-exports the ScriptureLoader from @bt-synergy/scripture-loader
 * This keeps the resource package self-contained while reusing the existing loader.
 */

export { ScriptureLoader } from '@bt-synergy/scripture-loader'
export type { ScriptureLoaderConfig } from '@bt-synergy/scripture-loader'
