# ARCHIVED — not on tc-study critical path

**Status:** Orphan / quarantine. `private: true`. **Do not grow or wire into tc-study.**

tc-study panel signals SoT: `@bt-synergy/resource-panels` (over npm `linked-panels`).

Leave until an explicit delete/revive decision. See `packages/README.md`.
