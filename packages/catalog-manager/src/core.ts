/**
 * Worker-safe CatalogManager entry — no React viewers / HMR.
 */
export { CatalogManager } from './CatalogManager'
export type {
  ResourceMetadata,
  SearchFilters,
  ProgressCallback,
  ResourceLoader,
  CatalogAdapter,
  CacheAdapter,
  CatalogConfig,
  ResourceLoaderPlugin,
} from './types'
